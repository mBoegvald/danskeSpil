"use strict";

window.addEventListener("DOMContentLoaded", init);

function init() {
  console.log("init");
  // INTRO ELLER START?
}

function clientInput() {
  // Mouse event listener
}

function mousePosition(event) {
  // get mouse position
}

function mouseCenter(x, y) {
  // center mouse
}

function moveStage() {
  // Move screen/stage
}

function checkShot() {
  // Get position of current target
  // Check if shot hit target
  // if hit start newTarget
}

function newTarget() {
  // let randomTarget = Math.random()*10;
  // EXAMPLE 10 TARGETS.
  // if(randomTarget > 0 && randomTarget < 1) {
  //     console.log("target1");
  // START FLIP UP ANIMATION
}

// document.onkeydown = function(evt) {
//   evt = evt || window.event;
//   var isEscape = false;
//   if ("key" in evt) {
//     isEscape = evt.key === "Escape" || evt.key === "Esc";
//   } else {
//     isEscape = evt.keyCode === 27;
//   }
//   if (isEscape) {
//     alert("Escape");
//   }
// };
